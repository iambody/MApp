<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.m.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ui.activity.ALoading">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".ui.activity.AMain"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.activity.Test.ATestRxJava"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.activity.Test.ATestRetorofit"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.Test.ASwipeToLayout"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.Test.AFragmentNavigator"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.Test.ANavigationView"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.Test.AGlide"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.Test.ARealm"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.Test.ADrawerLayout"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.Test.AToolBar"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.Test.ABottomNavigationBar"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.Test.AFileManger"
            android:launchMode="singleTask" />
    </application>

</manifest>